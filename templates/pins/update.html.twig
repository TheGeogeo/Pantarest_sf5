{% extends 'base.html.twig' %}

{% block title 'Edit ' ~ pin.title %}

{% block body %}
	<h1>Edit Pin</h1>

	{{form_start(form)}}
	{{form_widget(form)}}
	<input type="submit" value="Update pin">
	{{form_end(form)}}
{% endblock %}
